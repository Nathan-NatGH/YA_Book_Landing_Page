{
  "entities": {
    "Lead": {
      "title": "Lead",
      "description": "A potential reader who has signed up for updates.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the lead."
        },
        "first_name": {
          "type": "string",
          "description": "The first name of the lead."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time the lead was created."
        }
      },
      "required": ["email", "first_name", "createdAt"]
    }
  },
  "firestore": {
    "leads": {
      "schema": "Lead",
      "description": "Collection of leads who signed up for updates. The email address is used as the document ID to ensure uniqueness."
    }
  }
}
